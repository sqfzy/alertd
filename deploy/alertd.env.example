ALERTD_DINGTALK_TOKEN=replace-with-access-token
# 配置 default route 的 secret_env 时必须提供；IP 白名单 route 应在 TOML 省略 secret_env。
ALERTD_DINGTALK_SECRET=replace-with-signing-secret
# 示例 live-mm route；未配置该 route 时无需设置。
# LIVE_MM_DINGTALK_TOKEN=replace-with-access-token
# LIVE_MM_DINGTALK_SECRET=replace-with-signing-secret
